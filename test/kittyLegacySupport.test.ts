import { describe, test, expect } from "vitest";
import { checkParse } from "./helpers/helpers";

/**
 * Handles kitty sequences that are sent when the term supports the kitty protocol,
 * but has not requested it.
 */
describe("modifiers + keys", () => {
    test("home", () => {
        //
    });
});
